const PATH = {
  root: '/',
  component: '/components',
  mirage: '/mirage',
  locale: '/locale',
}

const paths = {
  root: PATH.root,
  qr: '/qr',
  payments: '/payments',
  auth: {
    signIn: '/sign-in',
    changePw: '/change-pw',
  },
}

export default paths
