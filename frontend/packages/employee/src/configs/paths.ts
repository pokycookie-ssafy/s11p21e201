const PATH = {
  root: '/',
}

const paths = {
  root: PATH.root,
  main: PATH.root,
  payments: '/payments',
  signIn: '/sign-in',
  setting: '/setting',
  changePw: '/change-pw',
}

export default paths
