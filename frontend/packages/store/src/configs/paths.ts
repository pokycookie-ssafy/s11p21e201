const PATH = {
  root: '/',
  overview: '/overview',
}

const paths = {
  root: PATH.root,
  main: '/sign-in',

  // auth
  signIn: '/sign-in',
  signUp: '/sign-up',

  // overview
  dashboard: `${PATH.overview}/dashboard`,
}

export default paths
